apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orderservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "orderservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orderservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
